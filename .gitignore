# dependencies
node_modules/
# build output
dist/
.astro/
# env & system
.env
.env.*
.DS_Store
Thumbs.db
# NOT: src/data/youtube-latest.json bilerek gitignore'da DEĞİL.
# YouTube RSS akışı GitHub Actions IP'lerine HTTP 404 dönüyor (yerelden 200),
# yani prebuild adımı canlı derlemede hep başarısız oluyordu ve anasayfa sabit
# banner'a düşüyordu. Dosya repoda durduğu için derleme onu buluyor; çekim
# başarılı olursa üzerine yazılır, başarısız olursa dokunulmaz.
# Tazelemek için: node scripts/fetch-youtube.mjs  (yerelde) sonra commit.

# görsel kurtarma raporu (yerelde üretilir)
migration/image-recovery-report.csv

# Melis'in gönderdiği ekran görüntüleri için geçici klasör (repoya girmez)
/img/
